# 🎉 Wan 2.5 模型升级说明

## 📅 更新时间
2025年10月21日

## 🆕 新增模型

### 1. wan2.5-t2v-preview (文本转视频)
**核心特性**：
- ✨ **音频生成支持**：自动配音或自定义音频，生成音画同步视频
- 📹 **分辨率**：480P、720P、**1080P** (全面支持高清)
- ⏱️ **时长**：**5秒或10秒** (从5秒提升到10秒)
- 🎞️ **帧率**：24fps (更流畅，接近电影级)
- 📝 **提示词长度**：最长**2000字符** (从1000提升)
- 📦 **输出格式**：MP4 (H.264编码)

**使用示例**：
```python
from src.text_to_video_service import TextToVideoService

service = TextToVideoService()
result = service.generate_video(
    prompt="一只可爱的小猫在花园里玩耍，阳光明媚",
    model="wan2.5-t2v-preview",
    duration=10,           # 10秒视频
    audio_enabled=True,    # 启用自动配音
    aspect_ratio="16:9"
)
```

### 2. wan2.5-i2v-preview (图像转视频)
**核心特性**：
- ✨ **音频生成支持**：同样支持音频能力
- 📹 **分辨率**：480P、720P、**1080P**
- ⏱️ **时长**：**5秒或10秒**
- 🎞️ **帧率**：24fps
- 📝 **提示词长度**：最长**2000字符**

**使用示例**：
```python
from src.image_to_video_service import ImageToVideoService

service = ImageToVideoService()
result = service.generate_video(
    image_file="path/to/image.jpg",
    prompt="图片中的人物微笑并挥手",
    model="wan2.5-i2v-preview",
    duration=10,           # 10秒视频
    audio_enabled=True,    # 启用自动配音
    audio_url="https://example.com/bgm.mp3"  # 可选：自定义音频
)
```

## 🎯 主要改进

### 1. 音频生成能力 🎵
- **自动配音**：AI 自动为视频生成匹配的音效和配音
- **自定义音频**：支持通过 URL 提供自定义背景音乐或音效
- **音画同步**：确保音频与视频内容完美同步

### 2. 更长视频时长 ⏰
- **10秒支持**：从原来的5秒提升到10秒
- **灵活选择**：可选择5秒或10秒，根据需求调整
- **费用计算**：费用 = 单价（基于分辨率）× 时长（秒）

### 3. 更高分辨率 📐
- **1080P全面支持**：所有 Wan 2.5 模型均支持 1080P
- **分辨率标识**：使用 "480P"、"720P"、"1080P" 标识（而非具体尺寸）
- **质量提升**：更清晰、更细腻的视频输出

### 4. 更长提示词 📝
- **2000字符**：从1000字符提升到2000字符
- **更详细描述**：支持更复杂、更详细的创作需求
- **更精确控制**：通过更长的提示词实现更精准的视频生成

### 5. 更流畅帧率 🎬
- **24fps**：提升至电影级帧率
- **更平滑**：动作更流畅，观看体验更佳

## 🔄 API参数变化

### 新增参数

#### 1. `duration` (时长)
- **类型**：`int`
- **可选值**：`5` 或 `10`（秒）
- **默认值**：`5`
- **说明**：视频生成时长

#### 2. `audio` (音频启用)
- **类型**：`bool`
- **默认值**：`True`
- **说明**：是否启用自动音频生成

#### 3. `audio_url` (自定义音频)
- **类型**：`str` (可选)
- **格式**：HTTP/HTTPS URL
- **说明**：自定义音频文件的公网 URL

#### 4. `resolution` (分辨率标识)
- **类型**：`str`
- **可选值**：`"480P"`, `"720P"`, `"1080P"`
- **说明**：Wan 2.5 使用分辨率标识，替代旧版的 `size` 参数

### 参数示例对比

**Wan 2.2（旧版）**：
```json
{
  "model": "wan2.2-t2v-plus",
  "input": {
    "prompt": "一只小猫在玩耍"
  },
  "parameters": {
    "size": "1920*1080"  // 使用具体尺寸
  }
}
```

**Wan 2.5（新版）**：
```json
{
  "model": "wan2.5-t2v-preview",
  "input": {
    "prompt": "一只小猫在花园里玩耍，阳光明媚"
  },
  "parameters": {
    "resolution": "1080P",    // 使用分辨率标识
    "duration": 10,           // 新增：时长
    "audio": true,            // 新增：音频开关
    "audio_url": "https://..."  // 可选：自定义音频
  }
}
```

## 📊 UI界面更新

### 新增控制组件

#### 1. 视频时长选择 ⏱️
- **位置**：文本转视频 & 图像转视频区域
- **类型**：单选按钮 (Radio)
- **选项**：5秒、10秒
- **默认**：5秒

#### 2. 音频生成开关 🎵
- **位置**：文本转视频 & 图像转视频区域
- **类型**：复选框 (Checkbox)
- **默认**：已启用
- **说明**：仅 Wan 2.5 模型支持

#### 3. 自定义音频URL 🎧
- **位置**：文本转视频 & 图像转视频区域
- **类型**：文本输入框
- **格式**：HTTP/HTTPS URL
- **说明**：可选参数，用于提供自定义音频

## ⚙️ 配置更新

### 新增配置项

```python
# config.py 新增配置
DEFAULT_DURATION = 5  # 默认视频时长
DURATION_OPTIONS = [5, 10]  # 可选时长选项
DEFAULT_AUDIO_ENABLED = True  # 默认启用音频
```

### 默认模型更新

```python
# 默认模型升级为 Wan 2.5
DEFAULT_MODEL = "wan2.5-t2v-preview"  # 文本转视频
DEFAULT_IMAGE_TO_VIDEO_MODEL = "wan2.5-i2v-preview"  # 图像转视频
```

## ⚠️ 重要注意事项

### 1. API兼容性
- ⚠️ **暂不支持SDK**：Wan 2.5 当前仅支持 HTTP 直接调用
- ✅ **继续使用 requests**：项目已使用 `requests` 库，无需修改

### 2. 参数格式差异
- **Wan 2.5**：使用 `resolution: "1080P"` 格式
- **Wan 2.2及以下**：使用 `size: "1920*1080"` 格式
- 🔧 代码已自动处理兼容性

### 3. 费用计算
- 💰 **按时长计费**：费用 = 单价 × 时长（秒）
- ⏰ **10秒 = 2倍费用**：10秒视频费用是5秒的2倍
- 📊 **分辨率影响单价**：1080P > 720P > 480P

### 4. 音频文件要求
如果提供自定义音频 URL：
- ✅ 必须是公网可访问的 HTTP/HTTPS URL
- ✅ 支持常见音频格式（MP3、WAV 等）
- ✅ 建议文件大小不超过 10MB
- ⚠️ 音频时长应与视频时长匹配

## 🔄 向后兼容

### 旧模型继续可用
所有 Wan 2.2 及更早版本的模型仍然可用：
- ✅ `wan2.2-t2v-plus`
- ✅ `wan2.2-i2v-plus`
- ✅ `wan2.2-i2v-flash`
- ✅ `wanx2.1-*` 系列

### 平滑升级
- 🔄 默认模型已更新为 Wan 2.5
- 🔧 用户仍可手动选择旧模型
- 📦 新参数仅对 Wan 2.5 生效，不影响旧模型

## 📚 文档更新

已更新以下文档：
- ✅ `README.md` - 主要使用文档
- ✅ `doc/README_zh.md` - 中文文档
- ✅ `doc/API.md` - API文档
- ✅ `doc/API_zh.md` - 中文API文档
- ✅ `demo.py` - 演示脚本

## 🚀 立即体验

### 1. 启动应用
```bash
python main.py
```

### 2. 访问界面
打开浏览器访问：`http://localhost:7860`

### 3. 选择 Wan 2.5 模型
在模型下拉框中选择：
- `wan2.5-t2v-preview` (文本转视频)
- `wan2.5-i2v-preview` (图像转视频)

### 4. 配置音频参数
- 设置视频时长：5秒或10秒
- 启用音频生成
- （可选）提供自定义音频 URL

### 5. 生成视频
点击"生成视频"按钮，体验音画同步的精彩视频！

## 🎓 最佳实践

### 1. 提示词优化
- 📝 充分利用2000字符限制
- 🎯 提供详细的场景、动作、风格描述
- 🎨 明确指定音效和配乐需求

### 2. 时长选择
- ⚡ **5秒**：快速预览、简短动画
- 🎬 **10秒**：完整叙事、复杂场景

### 3. 音频策略
- 🤖 **自动配音**：适合大多数场景
- 🎵 **自定义音频**：品牌视频、特定氛围

### 4. 分辨率选择
- 📱 **480P**：社交媒体快速预览
- 📺 **720P**：标准清晰度
- 🎥 **1080P**：高质量输出

## 📞 技术支持

如有问题或建议：
- 📧 邮箱：support@example.com
- 💬 GitHub Issues: [提交问题](https://github.com/PCcoding666/WAN_GATEWAY/issues)
- 📖 官方文档：[阿里云 Wan 2.5 文档](https://help.aliyun.com/zh/model-studio/developer-reference/wan-api)

---

**祝您使用愉快！🎉**
